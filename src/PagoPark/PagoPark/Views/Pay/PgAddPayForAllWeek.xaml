<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="PagoPark.Views.PgAddPayForAllWeek"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:PagoPark.ViewModels"
    Title="Set payment for this week"
    x:DataType="vm:PgAddPayForAllWeekViwModel"
    BackgroundColor="#CC401A24"
    Shell.PresentationMode="ModalAnimated">

    <Frame
        Margin="14,10"
        Padding="14,10"
        VerticalOptions="Center">
        <Grid
            RowDefinitions="auto,auto,auto"
            RowSpacing="14"
            VerticalOptions="End">
            <Label
                Style="{StaticResource LabelTitle}"
                HorizontalOptions="Center"
                Text="Set payment for this week"
                VerticalOptions="Center" />
            <VerticalStackLayout Grid.Row="1" Spacing="16">
                <Grid RowDefinitions="auto, auto">
                    <Label
                        Style="{StaticResource LabelSubtitle}"
                        Grid.Row="0"
                        Text="Contract: *" />
                    <Picker
                        Title="Select a contract"
                        Grid.Row="1"
                        ItemsSource="{Binding ParkContracts, Mode=OneWay}"
                        SelectedItem="{Binding SelectedParkContract, Mode=TwoWay}" />
                </Grid>
                <Label
                    Style="{StaticResource LabelCaptionBody}"
                    Grid.Row="0"
                    HorizontalOptions="Center"
                    Text="{Binding AmountTotal, Mode=OneWay}"
                    TextTransform="Uppercase" />
                <Grid RowDefinitions="auto, auto">
                    <Label
                        Style="{StaticResource LabelSubtitle}"
                        Grid.Row="0"
                        Text="Amount: *" />
                    <Entry
                        Grid.Row="1"
                        Keyboard="Numeric"
                        Placeholder="0.00"
                        Text="{Binding Amonunt}" />
                </Grid>
                <Grid RowDefinitions="auto, auto">
                    <Label
                        Style="{StaticResource LabelSubtitle}"
                        Grid.Row="0"
                        Text="Observations:" />
                    <Editor
                        Grid.Row="1"
                        Placeholder="Write your observation here!"
                        Text="{Binding Observations}" />
                </Grid>
                <Label
                    FontSize="Body"
                    IsVisible="{Binding IsVisibleInfo, Mode=OneWay}"
                    TextColor="Red">
                    <Label.FormattedText>
                        <FormattedString>
                            <Span FontFamily="icofont" Text="{StaticResource InfoCircle}" />
                            <Span Text=" Enter the required data (*)" />
                        </FormattedString>
                    </Label.FormattedText>
                </Label>
            </VerticalStackLayout>
            <VerticalStackLayout Grid.Row="2" Spacing="10">
                <Button Command="{Binding SaveCommand}" Text="Save" />
                <Button Command="{Binding CancelCommand}" Text="Cancel" />
            </VerticalStackLayout>
        </Grid>
    </Frame>

</ContentPage>